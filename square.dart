void main() {
  // Assign values to length and breadth
  int length = 10;
  int breadth = 10;

  // Check if it's a square or rectangle
  if (length == breadth) {
    print("It's a square.");
  } else {
    print("It's a rectangle.");
  }
}